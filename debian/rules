#!/usr/bin/make -f
export PYBUILD_NAME=stek_plugin

%:
	dh $@ --with python3 --buildsystem=pybuild

# tests run in CI; skip during package build (avoids a build-time cryptography dep)
override_dh_auto_test:
